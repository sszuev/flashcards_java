app:
  loader:
    enabled: true
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    initialization-mode: always
  jpa: # temporary generate schema from JPA markup
    generate-ddl: true
    hibernate:
      ddl-auto: create
    show-sql: false
    properties:
      hibernate:
        format_sql: true
